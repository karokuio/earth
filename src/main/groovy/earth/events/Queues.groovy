package earth.events

/**
 * Message broker queues definitions
 *
 * @since 0.1.0
 */
class Queues {

  /**
   * Template messages queue
   *
   * @since 0.1.0
   */
  static final String TEMPLATES = 'templates'

  /**
   * Docker messages queue
   *
   * @since 0.1.0
   */
  static final String DOCKER = 'docker'

  /**
   * All events messages queue
   *
   * @since 0.1.0
   */
  static final String EVENTS = 'events'
}
